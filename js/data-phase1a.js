// ═══ PHASE 1: Foundation — Days 1-15 ═══
// Each day: 3 DSA + 1 LLD + 1 HLD + 1 Custom DS + 1 Leadership

DAYS_DATA.push(
{day:1,title:"Arrays, Strategy Pattern & URL Shortener",challenges:[
{id:"d1c1",category:"dsa",difficulty:"medium",title:"Two Sum — All Pairs",description:"Given an array and target, return ALL unique pairs that sum to target. Handle duplicates efficiently.",hints:["Sort + two pointers gives O(n log n)","Skip duplicate values while moving pointers","HashSet approach gives O(n) but needs dedup"],approach:"Sort array. Two pointers lo=0, hi=n-1. If sum==target, add pair, skip duplicates on both sides. If sum<target, lo++. If sum>target, hi--. Time: O(n log n).",companies:["Amazon","Google","Microsoft"],code:"public List<int[]> twoSumPairs(int[] nums, int target) {\n    Arrays.sort(nums);\n    List<int[]> result = new ArrayList<>();\n    int lo = 0, hi = nums.length - 1;\n    while (lo < hi) {\n        int sum = nums[lo] + nums[hi];\n        if (sum == target) {\n            result.add(new int[]{nums[lo], nums[hi]});\n            while (lo < hi && nums[lo] == nums[lo+1]) lo++;\n            while (lo < hi && nums[hi] == nums[hi-1]) hi--;\n            lo++; hi--;\n        } else if (sum < target) lo++;\n        else hi--;\n    }\n    return result;\n}",
tradeoffs:"<strong>HashSet (O(N) Time, O(N) Space)</strong> vs <strong>Two Pointers + Sort (O(N log N) Time, O(1) Space)</strong>.<br>If the array is already sorted, two pointers is strictly better. If memory is heavily constrained (e.g., embedded systems), sort in place and use two pointers. If speed is the absolute only priority and RAM is unlimited, use a HashMap.",
redFlags:"<li>Failing to mention the edge case of integer overflow if adding massive integers.</li><li>Creating a Set of Lists to deduplicate instead of moving pointers, which wastes massive amounts of memory.</li>"
},
{id:"d1c2",category:"dsa",difficulty:"medium",title:"Container With Most Water",description:"Given heights array, find two lines that form a container holding the most water.",hints:["Two pointers from both ends","Move the shorter line inward — it limits height","Track max area = min(h[l],h[r]) * (r-l)"],approach:"Two pointers l=0, r=n-1. Area = min(h[l],h[r])*(r-l). Move the pointer with smaller height. Greedy: moving shorter line may find taller one. O(n) time, O(1) space.",companies:["Amazon","Google","Meta"],code:"public int maxArea(int[] h) {\n    int l = 0, r = h.length - 1, max = 0;\n    while (l < r) {\n        max = Math.max(max, Math.min(h[l], h[r]) * (r - l));\n        if (h[l] < h[r]) l++; else r--;\n    }\n    return max;\n}"},
{id:"d1c3",category:"dsa",difficulty:"medium",title:"Maximum Subarray (Kadane's Algorithm)",description:"Find the contiguous subarray with the largest sum. Follow-up: Return the actual subarray, not just the sum.",hints:["Kadane's: current_sum = max(num, current_sum + num)","Reset when current_sum goes negative","Track start/end indices for follow-up"],approach:"Kadane's Algorithm: iterate, maintain currentSum. If currentSum + num > num, extend subarray. Else start new subarray at num. Track maxSum globally. For indices: record start when resetting, record end when updating max. O(n).",companies:["Amazon","Microsoft","Google"],code:"public int maxSubArray(int[] nums) {\n    int maxSum = nums[0], curSum = nums[0];\n    for (int i = 1; i < nums.length; i++) {\n        curSum = Math.max(nums[i], curSum + nums[i]);\n        maxSum = Math.max(maxSum, curSum);\n    }\n    return maxSum;\n}"},
{id:"d1c4",category:"lld",difficulty:"medium",title:"Design Notification System — Strategy Pattern",description:"Design a notification system where messages can be sent via Email, SMS, Push, or WhatsApp. Adding a new channel should require ZERO changes to existing code.",hints:["NotificationStrategy interface with send(User, Message)","Each channel implements the interface independently","Map<Channel, Strategy> for lookup, or use Factory"],approach:"Interface: NotificationStrategy { void send(User u, Message m); }. Implementations: EmailStrategy, SmsStrategy, PushStrategy. NotificationService holds a Map<NotificationType, Strategy>. Open/Closed: adding Slack = 1 new class, 0 existing changes. Use @Component + @Qualifier in Spring.",companies:["Uber","Swiggy","Flipkart"],
rubric:"<ul><li><strong>Core Pattern (40%)</strong>: Successfully defines interface and isolated implementations.</li><li><strong>Extensibility (30%)</strong>: Demonstrates how adding \"Telegram\" requires no changes to NotificationService.</li><li><strong>Dependency Injection (30%)</strong>: Uses a factory or Spring Map to inject strategies dynamically.</li></ul>",
redFlags:"<li>Using massive <code>if/else</code> or <code>switch</code> blocks in a central service (Violates Open/Closed Principle).</li><li>Mixing business logic (e.g., fetching user phone numbers) inside the strategy classes.</li>",
tradeoffs:"<strong>Strategy Pattern</strong> vs <strong>Chain of Responsibility</strong>.<br>Strategy is best when EXACTLY ONE algorithm is selected at runtime. Chain of Responsibility is better if a notification might fall back (e.g., try Push, if offline try SMS)."
},
{id:"d1c5",category:"hld",difficulty:"hard",title:"Design URL Shortener — Back of Envelope",description:"Design TinyURL. 100M URLs/month, 10:1 read-write ratio. Calculate storage, QPS, cache for 5 years.",hints:["Write QPS ≈ 38/s, Read QPS ≈ 380/s","7-char Base62 = 3.5 trillion combinations","Cache top 20% URLs (Pareto principle)"],approach:"Base62 encoding (7 chars). Counter-based ID (distributed: Snowflake) or hash-based. Storage: 500 bytes/entry × 6B entries (5yr) = 3TB. Cache: Redis for hot URLs, 20% = ~600GB. DB: Cassandra/DynamoDB. Read path: Cache → DB. Write: generate short URL, store mapping.",companies:["Google","Amazon","Meta"],
rubric:"<ul><li><strong>Estimations (20%)</strong>: Calculates 3TB storage and ~400 QPS correctly.</li><li><strong>Algorithm (30%)</strong>: Defends Base62 encoding over MD5 hashing (avoids collisions/retries).</li><li><strong>ID Generation (30%)</strong>: Avoids auto-increment DB bottleneck (proposes Snowflake or pre-allocated ranges).</li><li><strong>Caching (20%)</strong>: Applies 80/20 rule for Redis sizing.</li></ul>",
redFlags:"<li>Proposing a single centralized MySQL auto-increment ID generation for a globally distributed system.</li><li>Using a 128-bit hash like MD5 and truncating it to 7 chars without handling collision retries securely.</li>",
tradeoffs:"<strong>Counter-based (Snowflake)</strong> vs <strong>Hash-based (MD5)</strong>.<br>Counter guarantees uniqueness immediately without DB roundtrips for collisions, but URLs are predictable. Hashes are unpredictable (secure) but require collision handling logic.",
pivots:"<strong>Interviewer:</strong> \"What if users start scraping our short URLs because they are predictable?\"<br><strong>Your Pivot:</strong> Move to Hash-based generation, or add a random salt to the Snowflake ID before Base62 encoding."
},
{id:"d1c6",category:"ds",difficulty:"medium",title:"Implement LRU Cache from Scratch",description:"Build LRU Cache with O(1) get and put using doubly linked list + HashMap. Don't use LinkedHashMap.",hints:["HashMap<Key, Node> for O(1) lookup","Doubly linked list for O(1) remove + add-to-head","Dummy head/tail nodes simplify edge cases"],approach:"Node{key,val,prev,next}. HashMap<Integer,Node>. get(): find in map, move to head, return val. put(): if exists, update+move; else create node, add to head, put in map. If over capacity: remove tail, delete from map. Always O(1).",companies:["Amazon","Google","Apple","Microsoft"],code:"class LRUCache {\n    Map<Integer,Node> map = new HashMap<>();\n    Node head = new Node(0,0), tail = new Node(0,0);\n    int cap;\n    LRUCache(int c) { cap=c; head.next=tail; tail.prev=head; }\n    int get(int key) {\n        if (!map.containsKey(key)) return -1;\n        Node n = map.get(key);\n        remove(n); addToHead(n);\n        return n.val;\n    }\n    void put(int key, int val) {\n        if (map.containsKey(key)) remove(map.get(key));\n        Node n = new Node(key, val);\n        addToHead(n); map.put(key, n);\n        if (map.size() > cap) {\n            Node last = tail.prev;\n            remove(last); map.remove(last.key);\n        }\n    }\n}"},
{id:"d1c7",category:"lead",difficulty:"medium",title:"Tell me about a time you pushed back on a requirement",description:"STAR Format: Product team wants a feature that would create tech debt or is infeasible. How did you handle it?",hints:["Lead with data, not opinions or ego","Propose alternatives, don't just say no","Show the outcome preserved the relationship"],approach:"S: Tight deadline + complex feature request. T: Evaluate feasibility and communicate trade-offs. A: Quantified impact ('adds 3 weeks, creates coupling with X'), proposed simpler MVP alternative. R: Agreed on MVP scope, delivered on time, planned V2. Key: Think business + engineering, always offer options.",companies:["All companies"],
redFlags:"<li><strong>The \"Rockstar\" Ego:</strong> \"I told the PM it was a stupid idea because they don't understand code.\"</li><li><strong>Lack of Compromise:</strong> Just saying no without offering a V1/V2 phased approach.</li><li><strong>Escalation First:</strong> Immediately running to the engineering manager instead of trying to align with the product manager first.</li>",
rubric:"<ul><li><strong>Business Empathy:</strong> Shows understanding of *why* the PM wanted the feature.</li><li><strong>Data-driven:</strong> Uses time/complexity estimates as the logic for pushing back, not personal preference.</li><li><strong>Constructive outcome:</strong> The story ends with a successful alternative delivered, not a canceled project.</li></ul>"
}
]},

{day:2,title:"Hashing, Observer Pattern & Chat System",challenges:[
{id:"d2c1",category:"dsa",difficulty:"medium",title:"Group Anagrams",description:"Given an array of strings, group anagrams together. 'eat','tea','ate' → same group.",hints:["Sort each word as key → O(n·k·log k)","Or use char frequency as key → O(n·k)","Map<String, List<String>>"],approach:"For each word: sort it (or build frequency key like '1a2b'). Use as HashMap key. Group words with same key. Sorting approach: O(n·k·log k). Frequency approach: O(n·k) where k = max word length.",companies:["Amazon","Google","Meta"],code:"public List<List<String>> groupAnagrams(String[] strs) {\n    Map<String, List<String>> map = new HashMap<>();\n    for (String s : strs) {\n        char[] c = s.toCharArray();\n        Arrays.sort(c);\n        String key = new String(c);\n        map.computeIfAbsent(key, k -> new ArrayList<>()).add(s);\n    }\n    return new ArrayList<>(map.values());\n}"},
{id:"d2c2",category:"dsa",difficulty:"medium",title:"Top K Frequent Elements",description:"Given an integer array, return the k most frequent elements. Must be better than O(n log n).",hints:["HashMap for frequency, then min-heap of size k","Bucket sort: index = frequency → O(n)","Quick select also works for O(n) average"],approach:"Approach 1: HashMap + min-heap size k → O(n log k). Approach 2: Bucket sort — create array of size n+1, bucket[freq] = list of nums with that freq. Iterate from high freq to low, collect k elements. O(n).",companies:["Amazon","Google","Meta"],code:"public int[] topKFrequent(int[] nums, int k) {\n    Map<Integer,Integer> freq = new HashMap<>();\n    for (int n : nums) freq.merge(n, 1, Integer::sum);\n    List<Integer>[] buckets = new List[nums.length + 1];\n    for (var e : freq.entrySet()) {\n        int f = e.getValue();\n        if (buckets[f] == null) buckets[f] = new ArrayList<>();\n        buckets[f].add(e.getKey());\n    }\n    int[] res = new int[k]; int idx = 0;\n    for (int i = buckets.length-1; i >= 0 && idx < k; i--)\n        if (buckets[i] != null)\n            for (int n : buckets[i]) { res[idx++] = n; if(idx==k) break; }\n    return res;\n}"},
{id:"d2c3",category:"dsa",difficulty:"medium",title:"Valid Sudoku",description:"Determine if a 9x9 Sudoku board is valid. Only validate filled cells, not whether the board is solvable.",hints:["Check each row, column, and 3×3 box","Use HashSet per row, column, and box","Box index = (row/3)*3 + col/3"],approach:"3 arrays of HashSets: rows[9], cols[9], boxes[9]. For each cell (i,j): if not '.', check if val exists in rows[i], cols[j], or boxes[(i/3)*3+j/3]. If yes → invalid. Else add to all three sets. O(81) = O(1).",companies:["Amazon","Microsoft","Uber"],code:"public boolean isValidSudoku(char[][] board) {\n    Set<String> seen = new HashSet<>();\n    for (int i = 0; i < 9; i++)\n        for (int j = 0; j < 9; j++) {\n            char c = board[i][j];\n            if (c == '.') continue;\n            if (!seen.add(c+\"r\"+i) || !seen.add(c+\"c\"+j)\n                || !seen.add(c+\"b\"+i/3+j/3)) return false;\n        }\n    return true;\n}"},
{id:"d2c4",category:"lld",difficulty:"medium",title:"Design Event Bus — Observer Pattern",description:"Design an in-process pub-sub event bus. Support: multiple subscribers per event type, async dispatch, unsubscribe.",hints:["Map<EventType, List<Consumer>> for subscribers","CopyOnWriteArrayList for thread-safe iteration","Submit callbacks to ExecutorService for async"],approach:"EventBus with ConcurrentHashMap<Class<?>, CopyOnWriteArrayList<Consumer>>. subscribe(Class<E>, Consumer<E>): add to list. publish(event): lookup by event.getClass(), invoke each consumer. Async: wrap dispatch in CompletableFuture.runAsync(). Unsubscribe: remove from list by reference.",companies:["Uber","LinkedIn","Netflix"]},
{id:"d2c5",category:"hld",difficulty:"hard",title:"Design Chat System (WhatsApp)",description:"1-1 messaging, group chats (256 max), online status, message delivery receipts. 500M DAU.",hints:["WebSocket for real-time bidirectional communication","Message queue for offline delivery","Fan-out on write for small groups"],approach:"Client → WebSocket Gateway → Message Service → Kafka → Recipient Gateway. Offline: store in Cassandra (write-heavy). Groups: fan-out on write (small) vs fan-out on read (large). Status: heartbeat every 30s to Redis. Delivery: sent/delivered/read receipts via separate channel. Message ordering: sequence number per conversation.",companies:["Meta","Google","Microsoft"]},
{id:"d2c6",category:"ds",difficulty:"medium",title:"Implement Min Stack with O(1) getMin",description:"Stack supporting push, pop, top, and retrieving minimum element — all in O(1) time.",hints:["Two stacks: main + minStack","minStack pushes min(val, current min)","Or single stack with math encoding trick"],approach:"Two-stack: push val to main, push min(val, minStack.peek()) to minStack. Pop from both. getMin = minStack.peek(). Space: O(n). Single-stack trick: if val < min, push 2*val - min (encoded marker), update min = val. On pop: if top < min, previous min = 2*min - top.",companies:["Amazon","Google","Goldman Sachs"],code:"class MinStack {\n    Deque<Integer> stack = new ArrayDeque<>();\n    Deque<Integer> minStack = new ArrayDeque<>();\n    void push(int val) {\n        stack.push(val);\n        minStack.push(minStack.isEmpty() ? val : Math.min(val, minStack.peek()));\n    }\n    void pop() { stack.pop(); minStack.pop(); }\n    int top() { return stack.peek(); }\n    int getMin() { return minStack.peek(); }\n}"},
{id:"d2c7",category:"lead",difficulty:"medium",title:"How do you handle an underperforming team member?",description:"A team member consistently misses deadlines or produces low-quality work. Describe your approach.",hints:["Start with empathy — understand root cause","Private 1:1, not public criticism","Set measurable improvement milestones"],approach:"1) Private 1:1 to understand blockers (personal? skill gap? unclear requirements?) 2) Co-create improvement plan with specific goals and timeline 3) Provide support: pairing, mentoring, reduced scope 4) Weekly check-ins to track progress 5) Document everything. Show you GREW someone, not just managed them out.",companies:["All companies"]}
]},

{day:3,title:"Two Pointers, Factory Pattern & Rate Limiter",challenges:[
{id:"d3c1",category:"dsa",difficulty:"medium",title:"3Sum",description:"Find all unique triplets [a,b,c] where a+b+c=0. No duplicate triplets allowed.",hints:["Sort + fix one element + two pointers for remaining","Skip duplicates of the fixed element","After finding triplet, skip duplicates of both pointers"],approach:"Sort array. For each i: skip if nums[i]==nums[i-1]. Two pointers lo=i+1, hi=n-1. If sum==0, add triplet, skip duplicates. If sum<0, lo++. If sum>0, hi--. O(n²).",companies:["Amazon","Google","Meta","Microsoft"],code:"public List<List<Integer>> threeSum(int[] nums) {\n    Arrays.sort(nums);\n    List<List<Integer>> res = new ArrayList<>();\n    for (int i = 0; i < nums.length - 2; i++) {\n        if (i > 0 && nums[i] == nums[i-1]) continue;\n        int lo = i+1, hi = nums.length-1;\n        while (lo < hi) {\n            int sum = nums[i]+nums[lo]+nums[hi];\n            if (sum == 0) {\n                res.add(List.of(nums[i],nums[lo],nums[hi]));\n                while (lo<hi && nums[lo]==nums[lo+1]) lo++;\n                while (lo<hi && nums[hi]==nums[hi-1]) hi--;\n                lo++; hi--;\n            } else if (sum < 0) lo++; else hi--;\n        }\n    }\n    return res;\n}"},
{id:"d3c2",category:"dsa",difficulty:"hard",title:"Trapping Rain Water",description:"Given elevation map, compute how much rain water it can trap between bars.",hints:["For each index: water = min(maxLeft, maxRight) - height","Two pointer approach gets O(1) space","Precompute maxLeft[] and maxRight[] for O(n) space approach"],approach:"Two pointers lo=0, hi=n-1, maxL=0, maxR=0. If h[lo]<h[hi]: maxL=max(maxL,h[lo]), water+=maxL-h[lo], lo++. Else symmetric for right. Time: O(n), Space: O(1).",companies:["Google","Amazon","Meta","Goldman Sachs"],code:"public int trap(int[] h) {\n    int lo = 0, hi = h.length-1, maxL = 0, maxR = 0, water = 0;\n    while (lo < hi) {\n        if (h[lo] < h[hi]) {\n            maxL = Math.max(maxL, h[lo]);\n            water += maxL - h[lo++];\n        } else {\n            maxR = Math.max(maxR, h[hi]);\n            water += maxR - h[hi--];\n        }\n    }\n    return water;\n}"},
{id:"d3c3",category:"dsa",difficulty:"medium",title:"Product of Array Except Self",description:"Return array where output[i] = product of all elements except nums[i]. No division allowed. O(n) time.",hints:["Prefix products from left, suffix products from right","First pass: left products. Second pass: multiply by right products","Use output array itself to save space"],approach:"Two passes: 1) Left to right: output[i] = product of all elements to left. 2) Right to left: multiply output[i] by product of all elements to right. Track running suffix product. O(n) time, O(1) extra space (output doesn't count).",companies:["Amazon","Google","Microsoft"],code:"public int[] productExceptSelf(int[] nums) {\n    int n = nums.length;\n    int[] res = new int[n];\n    res[0] = 1;\n    for (int i = 1; i < n; i++) res[i] = res[i-1] * nums[i-1];\n    int suffix = 1;\n    for (int i = n-2; i >= 0; i--) {\n        suffix *= nums[i+1];\n        res[i] *= suffix;\n    }\n    return res;\n}"},
{id:"d3c4",category:"lld",difficulty:"medium",title:"Design Payment Gateway — Factory + Strategy",description:"Support Razorpay, Stripe, PayPal. Factory creates processor; Strategy handles pay/refund logic.",hints:["PaymentProcessor interface: process(), refund(), getStatus()","Factory selects based on config/region","Idempotency key prevents double-charge"],approach:"Interface PaymentProcessor { PaymentResult process(Money amount); RefundResult refund(String txnId); }. RazorpayProcessor, StripeProcessor implementations. PaymentProcessorFactory.create(provider) returns correct impl. Store TransactionEntity for idempotency. Use @ConditionalOnProperty in Spring for auto-config.",companies:["Razorpay","Stripe","PhonePe","Paytm"]},
{id:"d3c5",category:"hld",difficulty:"medium",title:"Rate Limiter — Token Bucket vs Sliding Window",description:"Compare Token Bucket, Sliding Window Log, Sliding Window Counter, Fixed Window. Design for distributed system.",hints:["Token Bucket allows bursts, used by AWS API Gateway","Sliding Window Counter = best balance of accuracy & memory","Distributed: Redis INCR + EXPIRE or sorted sets"],approach:"Token Bucket: refill tokens at fixed rate, each request consumes 1. Sliding Window Counter: weighted sum of current + previous window counts. Distributed: Redis INCR+EXPIRE (fixed window) or ZADD+ZRANGEBYSCORE (sliding log). Recommendation: Sliding Window Counter balances accuracy and memory. Add retry-after header.",companies:["Google","Uber","Amazon","Cloudflare"]},
{id:"d3c6",category:"ds",difficulty:"hard",title:"Implement Trie with Wildcard Search",description:"Build Trie supporting insert(word), search(word), and search with '.' wildcard matching any character.",hints:["TrieNode has Map<Character, TrieNode> children + isEnd flag","For '.': recursively try ALL children","Prune early if no children exist"],approach:"TrieNode { Map<Char, TrieNode> children; boolean isEnd }. Insert: iterate chars, create nodes. Search: if char=='.', recurse into all children, return true if any matches. Else follow specific child. Time: O(m) exact, O(26^m) worst for wildcards.",companies:["Google","Amazon","Meta"],code:"class Trie {\n    TrieNode root = new TrieNode();\n    void insert(String word) {\n        TrieNode node = root;\n        for (char c : word.toCharArray())\n            node = node.children.computeIfAbsent(c, k -> new TrieNode());\n        node.isEnd = true;\n    }\n    boolean search(TrieNode node, String word, int i) {\n        if (i == word.length()) return node.isEnd;\n        char c = word.charAt(i);\n        if (c == '.') {\n            for (TrieNode child : node.children.values())\n                if (search(child, word, i+1)) return true;\n            return false;\n        }\n        return node.children.containsKey(c) && search(node.children.get(c), word, i+1);\n    }\n}"},
{id:"d3c7",category:"lead",difficulty:"hard",title:"Describe a technical decision with long-term architectural impact",description:"A tech choice (database, framework, architecture) that affected the system for years. What was the process?",hints:["Show you evaluated 3+ options with trade-offs","Mention stakeholder buy-in and ADR documentation","Discuss validation over time with metrics"],approach:"Context → Options (3+ with pros/cons) → Criteria (scale, team skills, cost, timeline) → Decision + Rationale → Outcome (metrics after 6-12 months). Show ADR (Architecture Decision Record) awareness. Best answers include: 'Here's what I'd do differently knowing what I know now.'",companies:["All FAANG","All unicorns"]}
]},

{day:4,title:"Sliding Window, Singleton & Database Scaling",challenges:[
{id:"d4c1",category:"dsa",difficulty:"hard",title:"Minimum Window Substring",description:"Find smallest window in s containing all characters of t (including duplicates).",hints:["Two pointers + frequency map for t","Expand right to satisfy, shrink left to minimize","Track 'formed' count vs 'required' distinct chars"],approach:"Build freq map for t. Two pointers l,r. Expand r: add to window map, if freq matches, increment formed. When formed==required, try shrinking l. Track min window. Time: O(|s|+|t|), Space: O(|s|+|t|).",companies:["Google","Amazon","Meta"],code:"public String minWindow(String s, String t) {\n    Map<Character,Integer> need = new HashMap<>(), have = new HashMap<>();\n    for (char c : t.toCharArray()) need.merge(c, 1, Integer::sum);\n    int required = need.size(), formed = 0;\n    int[] ans = {-1, 0, 0}; // len, l, r\n    for (int l = 0, r = 0; r < s.length(); r++) {\n        char c = s.charAt(r);\n        have.merge(c, 1, Integer::sum);\n        if (need.containsKey(c) && have.get(c).intValue() == need.get(c).intValue()) formed++;\n        while (formed == required) {\n            if (ans[0] == -1 || r-l+1 < ans[0]) { ans[0]=r-l+1; ans[1]=l; ans[2]=r; }\n            char lc = s.charAt(l);\n            have.merge(lc, -1, Integer::sum);\n            if (need.containsKey(lc) && have.get(lc) < need.get(lc)) formed--;\n            l++;\n        }\n    }\n    return ans[0] == -1 ? \"\" : s.substring(ans[1], ans[2]+1);\n}"},
{id:"d4c2",category:"dsa",difficulty:"hard",title:"Sliding Window Maximum",description:"Given array and window size k, return max element in each sliding window position.",hints:["Brute force O(n·k). Can you do O(n)?","Monotonic decreasing deque — front is always max","Remove elements outside window from front, smaller elements from back"],approach:"Deque stores indices in decreasing order of values. For each element: 1) Remove front if outside window. 2) Remove from back while back value ≤ current (they'll never be max). 3) Add current to back. 4) Front of deque = max. O(n).",companies:["Amazon","Google","Microsoft"],code:"public int[] maxSlidingWindow(int[] nums, int k) {\n    Deque<Integer> dq = new ArrayDeque<>();\n    int[] res = new int[nums.length - k + 1];\n    for (int i = 0; i < nums.length; i++) {\n        while (!dq.isEmpty() && dq.peekFirst() < i-k+1) dq.pollFirst();\n        while (!dq.isEmpty() && nums[dq.peekLast()] <= nums[i]) dq.pollLast();\n        dq.offerLast(i);\n        if (i >= k-1) res[i-k+1] = nums[dq.peekFirst()];\n    }\n    return res;\n}"},
{id:"d4c3",category:"dsa",difficulty:"medium",title:"Longest Substring Without Repeating Characters",description:"Find the length of the longest substring without duplicate characters.",hints:["Sliding window with HashSet or HashMap","Expand right, if duplicate found shrink left","HashMap<char, lastIndex> allows jumping left pointer"],approach:"Sliding window. HashMap<Character, Integer> stores last seen index. For each r: if char seen and lastIndex >= l, move l = lastIndex + 1. Update map. Track maxLen = r - l + 1. O(n) time, O(min(n,26)) space.",companies:["Amazon","Google","Microsoft","Bloomberg"],code:"public int lengthOfLongestSubstring(String s) {\n    Map<Character,Integer> map = new HashMap<>();\n    int max = 0;\n    for (int l = 0, r = 0; r < s.length(); r++) {\n        if (map.containsKey(s.charAt(r)))\n            l = Math.max(l, map.get(s.charAt(r)) + 1);\n        map.put(s.charAt(r), r);\n        max = Math.max(max, r - l + 1);\n    }\n    return max;\n}"},
{id:"d4c4",category:"lld",difficulty:"medium",title:"Thread-Safe Singleton — 3 Approaches",description:"Implement Singleton: lazy, thread-safe, resistant to reflection/serialization attacks. Compare approaches.",hints:["Enum singleton is simplest and most robust","Bill Pugh: static inner class holder pattern","Double-checked locking needs volatile"],approach:"1) Enum: enum Singleton { INSTANCE } — best, prevents reflection+serialization. 2) Bill Pugh: private static class Holder { static final S INSTANCE = new S(); } — lazy, thread-safe via class loading. 3) Double-checked locking: volatile field + synchronized block. Discuss trade-offs in interview.",companies:["All companies"]},
{id:"d4c5",category:"hld",difficulty:"hard",title:"Database Sharding Strategies",description:"Compare Range, Hash, Directory, and Geo-based sharding. Discuss resharding and cross-shard queries.",hints:["Range: hotspots possible (celebrity users)","Hash: even distribution but range queries suffer","Consistent hashing avoids full resharding"],approach:"Range: userId 1-1M → shard1. Problem: hotspots. Hash: hash(key)%N. Problem: resharding moves everything. Solution: consistent hashing with virtual nodes. Geo: region-based for data sovereignty. Cross-shard joins: denormalize or app-level. Tools: Vitess, ProxySQL, Citus.",companies:["Google","Amazon","Uber","Flipkart"]},
{id:"d4c6",category:"ds",difficulty:"hard",title:"Implement Skip List",description:"Probabilistic DS with O(log n) search/insert/delete — like balanced BST but simpler to implement.",hints:["Multiple levels of sorted linked lists","Top levels are 'express lanes' skipping elements","Coin flip decides if element promoted to higher level"],approach:"Array of levels, each sorted linked list. Search: start top, move right until next > target, move down. Insert: find position at level 0, insert, randomly promote (p=0.5) to higher levels. Delete: remove from all levels. Expected O(log n) all ops.",companies:["Redis","LevelDB"]},
{id:"d4c7",category:"lead",difficulty:"medium",title:"How do you manage technical debt?",description:"Team has significant tech debt. Product keeps pushing features. How do you balance?",hints:["Quantify debt: 'adds 2 hours to every deploy'","20% rule: allocate sprint capacity to debt","Make debt visible to non-technical stakeholders"],approach:"1) Inventory + categorize debt (critical/high/medium/low). 2) Quantify business impact ('causes 2 outages/quarter'). 3) Negotiate 'tech debt budget' — 20% of sprint. 4) Piggyback: refactor code you're touching for features. 5) Track + celebrate reduction. Never ask for 'tech debt sprint' — it never gets approved.",companies:["All companies"]}
]},

{day:5,title:"Stacks, Builder Pattern & CDN Design",challenges:[
{id:"d5c1",category:"dsa",difficulty:"hard",title:"Largest Rectangle in Histogram",description:"Given bar heights, find the area of the largest rectangle that can be formed.",hints:["Monotonic increasing stack stores indices","Pop when current < stack top → calculate area","Width = i - stack.peek() - 1 (or i if stack empty)"],approach:"Stack stores indices in increasing height order. For each bar: while stack top is taller, pop and calc area = popped height × (i - stack.peek() - 1). Push current. After loop, process remaining. O(n).",companies:["Google","Amazon","Microsoft"],code:"public int largestRectangle(int[] h) {\n    Deque<Integer> st = new ArrayDeque<>();\n    int max = 0;\n    for (int i = 0; i <= h.length; i++) {\n        int cur = i == h.length ? 0 : h[i];\n        while (!st.isEmpty() && h[st.peek()] > cur) {\n            int height = h[st.pop()];\n            int width = st.isEmpty() ? i : i - st.peek() - 1;\n            max = Math.max(max, height * width);\n        }\n        st.push(i);\n    }\n    return max;\n}"},
{id:"d5c2",category:"dsa",difficulty:"medium",title:"Daily Temperatures — Monotonic Stack",description:"Given daily temperatures, for each day find how many days until a warmer day. Return 0 if never.",hints:["Iterate right to left with decreasing stack","Or iterate left to right with increasing stack of indices","Pop elements that current temp is warmer than"],approach:"Stack stores indices of unresolved days (decreasing temps). For each day: while stack top temp ≤ current, pop and set answer[popped] = i - popped. Push current index. O(n).",companies:["Amazon","Google","Goldman Sachs"],code:"public int[] dailyTemperatures(int[] temps) {\n    int[] res = new int[temps.length];\n    Deque<Integer> st = new ArrayDeque<>();\n    for (int i = 0; i < temps.length; i++) {\n        while (!st.isEmpty() && temps[st.peek()] < temps[i])\n            res[st.peek()] = i - st.pop();\n        st.push(i);\n    }\n    return res;\n}"},
{id:"d5c3",category:"dsa",difficulty:"medium",title:"Evaluate Reverse Polish Notation",description:"Evaluate arithmetic expression in RPN. Tokens: numbers and +, -, *, /.",hints:["Stack-based: push numbers, on operator pop 2 and compute","Order matters for - and /: second popped is left operand","Integer division truncates toward zero"],approach:"Stack of integers. For each token: if number, push. If operator, pop b then a, compute a op b, push result. Final answer is stack top. O(n) time, O(n) space.",companies:["Amazon","LinkedIn","Google"],code:"public int evalRPN(String[] tokens) {\n    Deque<Integer> st = new ArrayDeque<>();\n    for (String t : tokens) {\n        if (\"+-*/\".contains(t)) {\n            int b = st.pop(), a = st.pop();\n            switch(t) {\n                case \"+\": st.push(a+b); break;\n                case \"-\": st.push(a-b); break;\n                case \"*\": st.push(a*b); break;\n                case \"/\": st.push(a/b); break;\n            }\n        } else st.push(Integer.parseInt(t));\n    }\n    return st.peek();\n}"},
{id:"d5c4",category:"lld",difficulty:"medium",title:"Design Pizza Order — Builder Pattern",description:"Pizzas have base, size, sauce, cheese, multiple toppings. Builder for flexible construction + validation.",hints:["Private Pizza constructor, public static Builder inner class","Builder has withX() methods returning 'this' for chaining","build() validates required fields and returns immutable Pizza"],approach:"Pizza.Builder(Size.LARGE, Base.THIN_CRUST).withSauce(MARINARA).withCheese(MOZZARELLA).addTopping(MUSHROOM).build(). Validation: size and base required. Pizza is immutable (final fields). toString() for receipt printing. Pattern ensures valid objects.",companies:["Swiggy","Zomato","DoorDash"]},
{id:"d5c5",category:"hld",difficulty:"medium",title:"CDN Design & Cache Invalidation",description:"Design a CDN. Cover PoP placement, cache hierarchy (L1/L2/Origin), purge strategies, edge computing.",hints:["PoPs at ISP peering points for lowest latency","L1(edge) → L2(regional) → Origin, check each level","Purge: TTL + explicit API + versioned URLs"],approach:"User → GeoDNS → nearest PoP. Cache hit → serve. Miss → L2 regional → Origin. Cache key: URL + Vary headers. Invalidation: 1) TTL (stale-while-revalidate) 2) Purge API 3) Versioned URLs (app.v2.js). Edge computing: run logic at PoP (Cloudflare Workers). Consistency: eventual — OK for static assets.",companies:["Cloudflare","Akamai","Amazon"]},
{id:"d5c6",category:"ds",difficulty:"medium",title:"Implement Circular Buffer (Ring Buffer)",description:"Fixed-size buffer with enqueue, dequeue, peek, isFull, isEmpty. Used in producer-consumer & network buffering.",hints:["Array with head and tail pointers","tail = (tail+1) % capacity","Full when (tail+1) % cap == head"],approach:"Array of fixed size. head: read position, tail: write position. enqueue: arr[tail]=val, tail=(tail+1)%cap. dequeue: val=arr[head], head=(head+1)%cap. isFull: (tail+1)%cap==head. isEmpty: head==tail. For thread-safety: AtomicInteger or ReentrantLock.",companies:["Amazon","Qualcomm","Intel"],code:"class CircularBuffer<T> {\n    Object[] buf; int head=0, tail=0, cap;\n    CircularBuffer(int c) { cap=c+1; buf=new Object[cap]; }\n    void enqueue(T val) { buf[tail]=val; tail=(tail+1)%cap; }\n    T dequeue() { T v=(T)buf[head]; head=(head+1)%cap; return v; }\n    boolean isFull() { return (tail+1)%cap==head; }\n    boolean isEmpty() { return head==tail; }\n}"},
{id:"d5c7",category:"lead",difficulty:"hard",title:"How do you drive alignment across multiple teams?",description:"Leading a project requiring 3 teams to coordinate. How do you ensure alignment on goals, timelines, interfaces?",hints:["Start with shared RFC / design doc","Define API contracts between teams early","Weekly sync + async updates, clear escalation path"],approach:"1) Kick-off with all tech leads — align on scope, non-goals, timeline. 2) Shared design doc, each team owns section. 3) Define API contracts (OpenAPI spec) first, teams work in parallel. 4) Weekly 15-min cross-team sync + async Slack updates. 5) Risk register with owners. 6) Demo every 2 weeks. Over-communicate early, under-surprise late.",companies:["All FAANG"]}
]}
);
